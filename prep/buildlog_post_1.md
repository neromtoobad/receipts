# Build-log post #1 — fire on day 2, once the x402 tx hash exists

Required by the rules: two public posts, tagging `@sibylcap` and each claimed
partner. This is one of the two. The second is the trust map on day 8.

**Do not post until the tx hash is real.** Every number below is already measured
and lives in `proof/DOMAINS.md`; the hash is the only blank.

---

## Thread

**1/**

RECEIPTS: six AI pundits forecasting real matches, for ten days.

Same model. Same prompt. Same budget.

The only thing that separates them is a private map of which informants are worth
paying for, and that map lives entirely in @sibylcap memory.

Day 2. Evidence layer is live on @base.

**2/**

Every piece of evidence costs real money. There is no free path.

x402 on Base Sepolia, USDC per call, prices from 0.003 to 0.045.

First settlement: 0xb0cc50dbf1530884b0789f15b0498632bb50d6a74b74336b58659fefd864eebc

**3/**

Before writing a line of the agent we checked there was anything to learn at all.

6,462 real matches, six leagues, three seasons. Fit on one season, measured on two
it had never seen.

The spread is real. Regional desks score +0.10 to +0.12 inside their beat and
+0.01 to +0.04 outside it.

**4/**

Then we ran the deletion test on the corpus, before the build.

Three arms, same budget, same matches. Only the memory differs.

Domain-scoped memory: $61.93
No domain scoping:    $184.44
No memory at all:     $928.01

Same forecast quality. 15x the cost.

**5/**

The best domain in the league is the one where nothing works.

Every informant scores zero or negative skill on crypto direction. Funding rate,
volume regime, momentum. All noise.

So the lesson there is not which source to buy. It is that none of them are worth
buying, and to stop paying.

**6/**

That is the part a global ranking cannot hold.

An agent that learns "formline has positive skill" from football carries it into
crypto and pays for noise forever.

Only per-domain memory catches it. Which is the whole reason the memory has to be
structured rather than a log.

**7/**

Process dies after every forecast. Fresh boot, empty context, nothing carries but
what was written to disk.

Cold boot to decision: 13.8ms.

Ten days. Repo and standings going up as it runs.

@sibylcap @base

---

## If you want one post instead of a thread

> Building RECEIPTS for the @sibylcap hackathon: six AI pundits, same model, same
> prompt, same budget. The only edge is a private map of which informants are worth
> paying for.
>
> Before writing the agent we measured whether there was anything to learn. 6,462
> real matches, held-out seasons. Then ran the deletion test on the corpus:
>
> with domain-scoped memory $61.93
> without any memory $928.01
>
> Same forecast quality, 15x the cost.
>
> Evidence is x402-gated on @base, first settlement 0xb0cc50db…64eebc.

---

## Checks before posting

- [x] The tx hash resolves on Basescan Sepolia (verified: SUCCESS, block 46195402)
- [ ] Every number matches `proof/DOMAINS.md` exactly
- [ ] `@sibylcap` and `@base` both tagged
- [ ] No claim about Virtuals until an ACP job has actually run
- [ ] Repo link included and the repo is public
